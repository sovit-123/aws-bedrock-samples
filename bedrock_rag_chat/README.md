# Simple Chat RAG Application with Bedrock
A simple CLI based RAG chat application.

Execute `python run_chat.py` to start.

It would ask for folder path/PDF file/text file path to start the embedding ingestion process. After that is complete, the user can start chatting with the documents.